export default function SomaUm(props) {
   // props.numero ++ /* DESSE JEITO NAO LE NO OUTRO PROPRIO DO ARQUIVO, SÓ COMO ESTÁ DESCRITO ABAIXO no <h1>{props.numero + 1}</h1> */
    return (
        <div>
            <h1>{props.numero + 1}</h1>
        </div>
    )
}